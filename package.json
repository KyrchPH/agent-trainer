{
  "name": "agent-trainer",
  "version": "0.0.1",
  "private": true,
  "description": "Training data builder for the Livestreamer AI Agent",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev --workspace @agent-trainer/client\" \"npm run dev --workspace @agent-trainer/server\"",
    "dev:client": "npm run dev --workspace @agent-trainer/client",
    "dev:server": "npm run dev --workspace @agent-trainer/server",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
